//Using var(Only use var if you MUST support old browsers.)
//Using let(Only use let if you can't use const)
//Using const(Always use const if the value should not be changed)

var x = 100;
console.log(x);

const s = "String";
console.log(s);

s1 = "Sonu";
s2 = "HS";

s3 = s1 + " " + s2; //`+` is used to concatenate strings
console.log(s3);

var1 = 100;
var2 = 20;

var3 = var1 + var2; // `+` is used to add two numbers
console.log(var3);



