// Numbers:
let length = 16;
let weight = 7.5;
console.log(typeof(length));

// Strings:
let color = "Yellow";
let lastName = "Johnson";

// Booleans
let x = true;
let y = false;

//Empty values (a special value that represents an absence of value)
let car = "";    // The value is "", the typeof is "string"

//Undefined (a special value that represents an uninitialized variable)
let s;    // Value is undefined, type is undefined

//OBJECTS

let obj = { name: "sonu", id: 30 };
obj.id = 31; // the age property of obj is now 31
console.log(obj);

